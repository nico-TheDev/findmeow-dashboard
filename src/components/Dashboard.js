import * as React from "react";
import { Card, CardContent, CardHeader } from "@mui/material";

const Dashboard = () => (
    <Card>
        <CardHeader title="FindMeow Admin Dashboard" />
        <CardContent>Manage FindMeow</CardContent>
    </Card>
);

export default Dashboard;
